- name: Install MySQL Client
  ansible.builtin.dnf:
    name: mysql
    state: installed

- name: Load Schema
  ansible.builtin.shell: mysql -h mysql.prashdevops.online -uroot -pRoboShop@1 < /app/schema/{{component}}.sql
